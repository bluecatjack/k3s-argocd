apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gateway-external
  namespace: gateway
spec:
  addresses:
  - type: IPAddress
    value: 10.10.10.25
  gatewayClassName: cilium
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: "*.guilder.moonsuit.org"
      allowedRoutes:
        namespaces:
          from: All
    - name: https
      protocol: HTTPS
      port: 443
      hostname: "*.guilder.moonsuit.org"
      tls:
        certificateRefs:
          - name: cert-guilder
            kind: Secret
            group: "" # required
      allowedRoutes:
        namespaces:
          from: All
